{% extends 'base.html.twig' %}

{% block title %}Bienvenue sur mon blog{% endblock %}

{% block body %}
<div class="container py-5">
    <h1 class="text-center text-primary mb-4">Bienvenue sur mon blog ! ✨</h1>
    <p class="text-center fs-5 mb-5">
        Découvrez les derniers articles, partagez vos idées, et laissez-vous inspirer !
    </p>

    <div class="row row-cols-1 row-cols-md-3 g-4">
        {% for article in articles %}
        <div class="col">
            <div class="card h-100 shadow-sm">
                <div class="card-body">
                    <h5 class="card-title">{{ article.title }}</h5>
                     <p class="card-title">{{ article.updatedAt|date('d/m/Y à H:i')  }}</p> 
					<p class="card-text">{{ article.content|length > 200 ? article.content|slice(0, 200) ~ '...' : article.content }}</p>
                </div>
                <div class="card-footer text-end">
                    <a href="{{ path('app_article_show', { id: article.id }) }}" class="btn btn-primary">
                        Lire la suite
                    </a>
                </div>
            </div>
        </div>
        {% else %}
        <p class="text-center">Aucun article disponible pour le moment. Revenez bientôt !</p>
        {% endfor %}
    </div>
</div>
{% endblock %}
